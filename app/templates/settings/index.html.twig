{% extends 'layout/base.html.twig' %}

{% block title %}Settings{% endblock %}

{% block main %}
	<twig:Section id="user-settings" title="User Settings" full>
		{% block content %}
			{{ form(userForm) }}
		{% endblock %}
	</twig:Section>
	<twig:Section id="alerts-settings" title="Alerts Settings" full>
		{% block content %}
			{{ form(alertForm) }}
		{% endblock %}
	</twig:Section>
{% endblock %}
